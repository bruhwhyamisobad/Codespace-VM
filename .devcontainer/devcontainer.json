{
  "name": "Webtop Ubuntu KDE",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "remoteUser": "vscode",
  "features": {
    "docker-in-docker": "latest"
  },
  "mounts": [
    "source=${localWorkspaceFolder}/webtop-config,target=/config,type=bind"
  ],
  "postStartCommand": "bash -c 'if ! docker ps -q -f name=webtop | grep -q .; then docker run -d --name webtop --restart unless-stopped -p 3000:3000 -v ${localWorkspaceFolder}/webtop-config:/config ghcr.io/linuxserver/webtop:ubuntu-kde; fi'",
  "forwardPorts": [3000]
}
